# TODO

## Bootstrap follow-up

- [ ] Run `docker compose up --build` and verify the browser shows the backend
  health status in a Docker-enabled environment.
- [ ] Commit and push the initial bootstrap once runtime verification succeeds.
