# Frozen-v4 generation rendering and activation

The public workflow is rehearsed only with injected synthetic v4 preflight
evidence. Every non-synthetic preparation command fails closed with
`fresh_v4_preflight_required`. The candidate must not be edited in place to
resolve that binding; a later reviewed milestone must version a resolved
candidate after a real v4 preflight and its human review exist.
