Assume you cannot directly access Docker, the browser, or other privileged local resources.

When those resources are needed, provide the exact commands for the user to run and use the resulting output to continue the investigation.